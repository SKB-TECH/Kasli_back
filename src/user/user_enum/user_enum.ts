export enum sexe {
  HOMME = "Homme",
  FEMME = "Femme",
}

export enum statut {
  ACTIVE = 1,
  INACTIF = 0,
}

export enum role {
  ADMIN = "admin",
  EMPLOYEE = "employee",
  USER = "user",
}

export enum type_compte {
  COMMISSIONNAIRE = "commissionaire",
  SIMPLE = "simple",
}
